#include <stdio.h>
#include <string.h>

#define MAXLEN 10

int htoi(char s[]);
void main(){
    char c[MAXLEN];
    scanf("%s", c);
    printf("%d\n", htoi(c));
}

int htoi(char s[]) {
    int n = 0;
    for (int i = 0; i < s[i]; i++)
    {
        /* code */
    }

    return n;
    
}